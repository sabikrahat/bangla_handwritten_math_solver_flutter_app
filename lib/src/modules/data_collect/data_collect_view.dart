import 'package:flutter/material.dart';

class DataCollectView extends StatelessWidget {
  const DataCollectView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Data Collect View')),
      body: const Center(
        child: Text('Data Collect View'),
      ),
    );
  }
}
